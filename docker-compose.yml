version: '3'
services:
  app:
    build:
      context: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    networks:
      - mynetwork

  mongodb:
    image: mongo:3
    volumes:
      - ./data/db:/data/db
    networks:
      - mynetwork
    depends_on:
      - app

networks:
  mynetwork:
    driver: bridge
